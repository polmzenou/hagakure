security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email

    firewalls:
        dev:
            pattern: ^/(_profiler|_wdt|assets|build)/
            security: false
        
        # Firewall pour les routes d'authentification (login, register)
        auth:
            pattern: ^/api/(login|register)
            stateless: true
            security: false
        
        # Firewall principal pour l'API (JWT)
        api:
            pattern: ^/api
            stateless: true
            provider: app_user_provider
            jwt: ~
            
        main:
            lazy: true
            provider: app_user_provider

    # Hiérarchie des rôles
    role_hierarchy:
        ROLE_ADMIN: [ROLE_USER]

    access_control:
        # Routes publiques (accessible à tous)
        - { path: ^/api/login, roles: PUBLIC_ACCESS }
        - { path: ^/api/register, roles: PUBLIC_ACCESS }
        
        # Routes GET (liste et show) - accessibles publiquement (sans authentification)
        - { path: ^/api/(samourais|clans|weapons|battles|locations|timeline)$, roles: PUBLIC_ACCESS, methods: [GET] }
        - { path: ^/api/(samourais|clans|weapons|battles|locations|timeline)/\d+$, roles: PUBLIC_ACCESS, methods: [GET] }
        
        # Styles de combat - ADMIN uniquement (toutes méthodes)
        - { path: ^/api/styles, roles: ROLE_ADMIN }
        
        # Routes POST (création) - ADMIN uniquement
        - { path: ^/api/(samourais|clans|weapons|battles|styles|locations)$, roles: ROLE_ADMIN, methods: [POST] }
        
        # Routes PUT/PATCH (modification) - ADMIN uniquement
        - { path: ^/api/(samourais|clans|weapons|battles|styles|locations)/\d+$, roles: ROLE_ADMIN, methods: [PUT, PATCH] }
        
        # Routes DELETE (suppression) - ADMIN uniquement
        - { path: ^/api/(samourais|clans|weapons|battles|styles|locations)/\d+$, roles: ROLE_ADMIN, methods: [DELETE] }

        # Gestion des utilisateurs - ADMIN uniquement
        - { path: ^/api/users, roles: ROLE_ADMIN }

when@test:
    security:
        password_hashers:
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4
                time_cost: 3
                memory_cost: 10

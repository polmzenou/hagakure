<form method="post" action="{{ path('app_clan_delete', {'id': clan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clan.id) }}">
    <button class="btn">Delete</button>
</form>

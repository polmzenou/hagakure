<form method="post" action="{{ path('app_samourai_delete', {'id': samourai.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ samourai.id) }}">
    <button class="btn">Delete</button>
</form>

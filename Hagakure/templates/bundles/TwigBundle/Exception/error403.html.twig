{% extends 'base.html.twig' %}

{% block title %}403 - Accès Refusé{% endblock %}

{% block body %}
<div class="app">
    <div class="forbidden-container">
        <div class="forbidden-content">
            <div class="forbidden-icon">🚫</div>
            <h1 class="forbidden-title">403 - Accès Refusé</h1>
            <p class="forbidden-message">
                Vous n'avez pas les permissions nécessaires pour accéder à cette page.
            </p>
            <p class="forbidden-submessage">
                Seuls les administrateurs peuvent créer, modifier ou supprimer du contenu.
            </p>
            <div class="forbidden-actions">
                <a href="{{ path('app_react_home') }}" class="btn-primary">
                    Retour à l'accueil
                </a>
                <a href="{{ path('app_react_home') }}/login" class="btn-secondary">
                    Se connecter
                </a>
            </div>
        </div>
    </div>
</div>

<style>
.forbidden-container {
    min-height: calc(100vh - 200px);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem;
    background: linear-gradient(135deg, #f5f5f5 0%, #e8e8e8 100%);
}

.forbidden-content {
    background: white;
    border-radius: 16px;
    padding: 4rem 3rem;
    text-align: center;
    max-width: 600px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
}

.forbidden-icon {
    font-size: 6rem;
    margin-bottom: 2rem;
    animation: shake 0.5s ease-in-out;
}

@keyframes shake {
    0%, 100% { transform: translateX(0); }
    25% { transform: translateX(-10px); }
    75% { transform: translateX(10px); }
}

.forbidden-title {
    font-size: 2.5rem;
    color: #D0391B;
    font-family: 'YujiSyuku', serif;
    margin-bottom: 1rem;
    font-weight: 600;
}

.forbidden-message {
    font-size: 1.25rem;
    color: #2C2C2C;
    margin-bottom: 1rem;
    line-height: 1.6;
}

.forbidden-submessage {
    font-size: 1rem;
    color: #666;
    margin-bottom: 2.5rem;
    line-height: 1.6;
}

.forbidden-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
}

.btn-primary,
.btn-secondary {
    padding: 0.875rem 2rem;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    font-size: 1rem;
    font-family: 'YujiSyuku', serif;
    transition: all 0.3s ease;
    display: inline-block;
}

.btn-primary {
    background: #D0391B;
    color: white;
    box-shadow: 0 2px 8px rgba(208, 57, 27, 0.2);
}

.btn-primary:hover {
    background: #B02F16;
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(208, 57, 27, 0.3);
}

.btn-secondary {
    background: white;
    color: #D0391B;
    border: 2px solid #D0391B;
}

.btn-secondary:hover {
    background: #D0391B;
    color: white;
    transform: translateY(-2px);
}
</style>
{% endblock %}
